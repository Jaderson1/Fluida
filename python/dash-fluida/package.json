{
  "name": "dash-fluida-frontend",
  "private": true,
  "version": "0.1.0",
  "description": "Frontend build for the dash-fluida Dash component — not published to npm; the Python package (fluida_dash) is what gets distributed, with this bundle copied inside it.",
  "type": "module",
  "scripts": {
    "build": "tsup && node scripts/copy-bundle.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fluida/core": "workspace:*"
  },
  "devDependencies": {
    "@types/prop-types": "^15.7.15",
    "@types/react": "^19.2.17",
    "prop-types": "^15.8.1",
    "react": "^19.2.7",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
